'use strict';

const React = require('react');
const PropTypes = React.PropTypes;

class Main extends React.Component {
  render() {
    return (
      <div></div>
    );
  }
};

export default Main;
