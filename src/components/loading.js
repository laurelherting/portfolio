import React, { Component } from 'react';

const Loading = () => (
    render: () => {
        return (
            <div className='loading'>
            </div>
        );
    }
));

export default Loading;
